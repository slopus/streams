---
title: Why a Single Node
description: streams runs on one machine on purpose — the case for scaling up instead of out, what a single machine actually gives you today, the distributed-systems cost you avoid, and the honest availability tradeoff.
---

import { Callout, Cards } from 'nextra/components'

# Why a Single Node

streams runs on one machine, and that is the most consequential decision in the project. It
isn't a limitation we're working around — it's the choice that makes everything else
possible: the small API, the millisecond latencies, the crisp answer to "what survives a
crash." Choose distribution instead and you inherit its complexity in every one of those
places.

This page makes the case for the choice, and is honest about what it costs.

## Scaling out is usually a reflex, not a requirement

The instinct, the moment a system starts to matter, is to spread it across nodes. But "more
nodes" is rarely the answer to a question anyone has actually measured — it's a habit, and an
expensive one, because the cost lands on the part of the system you operate forever, not the
part you write once.

streams takes the opposite default: **use one machine well, and reach for more only once
you can show you've outgrown it.** For most event workloads, that day simply never arrives.

## A single machine is bigger than you think

The mental picture of "one server" is stuck a decade in the past. A box you can rent by the
hour today gives you, on a single predictable bill:

- **a hundred or more cores,**
- **multiple terabytes of RAM,** and
- **local NVMe sustaining millions of IOPS** at sub-millisecond latency.

That is more capacity than many distributed clusters had a few years ago. streams is built
to actually use it: the write-ahead log is [sharded across cores](/how-it-works/wal-and-group-commit)
so durable throughput scales with the hardware, reads are `O(1)` seq-indexed lookups, and a
record delivered live over SSE is [serialized once](/how-it-works/performance) and shared
across every watcher. The design targets — **~1,000,000 events/sec** batched and **~1 ms**
delivery — come *from* staying on one machine, not in spite of it.

## What you are not paying for

The real case for one node isn't the throughput — it's everything a distributed log spends
to reach its throughput that streams simply never spends:

- **No coordination on the write path.** No replication round-trips, no consensus, no
  partition or rebalance handling. A write is a sequential append to a local disk the engine
  fully owns. Every microsecond a cluster spends agreeing, streams spends appending — which is
  precisely why the latency floor is low enough to quote in milliseconds.
- **No distributed failure modes.** Split brain, partial failure, stale replicas, cross-node
  clock skew, "which node is authoritative" — none of these can occur when there is only one
  node. A whole category of 3 a.m. incidents is defined out of existence.
- **One thing to run.** A single process pointed at a single directory: one thing to deploy,
  monitor, secure, and back up — not a quorum to keep healthy or a membership protocol to
  babysit. The complexity you don't take on is the complexity you never debug.

<Callout type="info">
  None of this is free — it's a trade. You exchange horizontal scale and built-in high
  availability for latency, simplicity, and a system small enough to fully understand. The
  next section is the bill.
</Callout>

## The tradeoff: availability

Be clear-eyed about the cost. A single node is a single point of failure for *availability*:
it goes away for a few seconds during a deploy, a kernel patch, or the occasional crash.
streams doesn't pretend otherwise — it makes that downtime **cheap and safe** rather than
engineering it away:

- **Acknowledged work is never lost.** A `disk` or `fsync` write lands in the write-ahead log
  before the ack, so a restart *replays* it instead of dropping it. See
  [Durability](/guarantees/durability).
- **Recovery is fast and gated.** On start the engine loads its latest snapshot and replays
  the log forward; `GET /v0/ready` stays `503` until an acknowledged durable write is fully
  recovered, then flips to `200`, so traffic never reaches a half-recovered server. See
  [Recovery](/how-it-works/recovery).
- **Shutdown is clean.** On `SIGINT`/`SIGTERM` the server drains and checkpoints, so the next
  start has almost nothing left to replay.

The honest calculus: **a few seconds of rare, mostly-planned downtime on a system you
understand, against the permanent complexity and subtler failures of one that is "always
up."** For most workloads that trade is worth making — and where it isn't, you mitigate at
the edges (a warm standby behind a proxy, frequent backups) instead of dragging a consensus
protocol into your data path.

## If you genuinely need more than one node

Don't argue yourself out of a real requirement. If you need transparent failover,
multi-region writes, or throughput beyond the largest single box, streams is **not** that
system — multi-server, replication, HA, and single-writer fencing are
[out of scope by design](/getting-started#whats-implemented-today), not unfinished features.

What you *can* build on top of independent nodes:

- **Asynchronous mirroring with [routers](/api/routers).** Because a record's origin node
  rides through every forward and [loop-prevention](/guarantees/node-loop-prevention) drops a
  node's own events, several nodes can mirror to each other without echo — handy for read
  fan-out and geographic locality. It buys you copies and proximity, not consistency or
  failover. See the [multi-master guide](/guides/multi-master).
- **Partitioning by topic across machines** at the application layer, when separate workloads
  don't need to share a single log.

<Callout type="warning">
  Router mirroring is **asynchronous and at-least-once** — it gives you copies and locality,
  never an availability or consistency guarantee. Don't mistake it for HA.
</Callout>

## Where to go next

<Cards>
  <Cards.Card title="Architecture Overview" href="/how-it-works" arrow>
    The single-machine, sequential-disk assumptions and the layered HTTP → engine → WAL → segments shape.
  </Cards.Card>
  <Cards.Card title="Performance" href="/how-it-works/performance" arrow>
    The throughput and latency targets, the measured baselines, and their honest caveats.
  </Cards.Card>
  <Cards.Card title="Running streams" href="/deployment" arrow>
    How the durable server starts, recovers, binds, and shuts down.
  </Cards.Card>
  <Cards.Card title="Durability" href="/guarantees/durability" arrow>
    Why a restart never loses acknowledged work on a `disk` or `fsync` topic.
  </Cards.Card>
</Cards>
